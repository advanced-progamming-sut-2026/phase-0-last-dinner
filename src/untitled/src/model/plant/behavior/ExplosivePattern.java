package model.plant.behavior;

public enum ExplosivePattern {
    RADIUS,
    CONTACT_RADIUS,
    CONTACT_SINGLE,
    FULL_LANE,
    FULL_BOARD,
    TERRAIN_ONLY,
    GRAVE_ONLY,
    RANDOM_TARGETS
}
